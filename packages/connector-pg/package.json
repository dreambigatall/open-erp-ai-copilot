{
  "name": "@erp-copilot/connector-pg",
  "version": "0.0.1",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "@erp-copilot/types": "*"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0"
  }
}
